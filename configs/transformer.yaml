ENV_ID: "LunarLander-v3"
NUM_ENVS: 8
VECTOR_ENV: "sync"
FRAME_SIZE: 128
STACK_SIZE: 16
MAX_EPISODE_STEPS: 250
SEED: 7

DEVICE: "auto"
USE_MIXED_PRECISION: true
USE_TORCH_COMPILE: false
PIN_MEMORY: true
USE_MLFLOW: false
OUTPUT_DIR: "runs"
CHECKPOINT_DIR: "checkpoints"
LOG_INTERVAL: 1

model_name: "Conv3DTransformerNet"
PRETRAINED_BACKBONE: false
HIDDEN_SIZE: 256
GRU_LAYERS: 1

LEARNING_RATE: 0.001
BACKBONE_LEARNING_RATE:
GAMMA: 0.99
GAE_LAMBDA: 0.95
VALUE_COEFF: 0.5
ENTROPY_COEFF: 0.01
CLIP_EPSILON: 0.2
TARGET_KL:
PPO_EPOCHS: 4
BATCH_SIZE: 512
NUM_ROUNDS: 10000
NUM_STEPS_PER_ROLLOUT: 1024
MAX_GRAD_NORM: 1.0
CHECKPOINT_FREQUENCY: 50
CHECKPOINT_PATH:
